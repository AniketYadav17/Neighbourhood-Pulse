[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "neighbourhood-pulse"
version = "0.1.0"
description = "Spatiotemporal gentrification predictor for London"
requires-python = ">=3.8"


[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]